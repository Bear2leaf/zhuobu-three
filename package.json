{
    "name": "zhuobu-three",
    "version": "0.0.1",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "esbuild src/main.ts src/worker/main.ts --outdir=dist  --target=es2015 --platform=node --outdir=dist --bundle --format=esm",
        "dev": "esbuild --sourcemap src/main.ts src/worker/main.ts --platform=node  --outdir=dist --bundle --format=esm --watch  --servedir=."
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/three": "^0.169.0",
        "esbuild": "^0.21.5",
        "minigame-api-typings": "^3.8.4"
    },
    "dependencies": {
        "@dimforge/rapier3d": "^0.14.0",
        "@dimforge/rapier3d-compat": "^0.14.0",
        "three": "^0.169.0"
    }
}
